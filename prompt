CUSTOMER WEB:
  in the entire customer web, the ticketPrice = platform fees + original ticket price by company.
  
  - If the customer booked a ticket:
      if  booked 1 ticke:
        will pay ticketPrice
      if booked 2 or more ticket:
        will pay ticketPrice * number of tickets
  - ticketPrice should be in booking's steps as welll 


ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY ONLY.
STOP.